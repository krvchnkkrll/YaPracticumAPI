# YaPracticumApi
## REST API для управления событиями

Проект на .net 9

### Сборка/запуск

build: ```dotnet build```

run: ```dotnet run --project Web```

tests: ```dotnet test```

listening: ```https://localhost:7013```

swagger: ```https://localhost:7013/swagger```

### API

- GET /api/events - возвращает пагинируемую коллекцию событий. (При запуске уже есть добавленные)
- GET /api/events/{id} - возвращает событие
- POST /api/events - создает и возвращает событие.
- PUT /api/events/{id} - изменяет и возвращает событие.
- DELETE /api/events/{id} - удаляет событие.

### Формат ошибок
```
{
  "title": "", - Название ошибки
  "status": 404, - Статус код ошибки
  "detail": "" - Текстовое описание ошибки
}
```

#### p.s.
1) После добавления пагинации был полностью убран endpoint возвращающий события без пагинации.
Мне кажется это правильным действием. Но по заданию надо было написать тесты:
- фильтрация по названию;
- фильтрация по датам (startDate, endDate);
- комбинированная фильтрация.
В этих тестах всегда присутствует пагинация, т.к. она обязательна.
2) Для теста "получение всех событий" добавил в EventService метод возвращающий все события без пагинации.
Но не стал добавлять endpoint.
